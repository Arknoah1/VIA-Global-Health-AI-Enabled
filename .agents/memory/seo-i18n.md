# Client-side i18n and SEO

If a site changes languages only in the browser on the same URL, search engines and AI crawlers do not get real language-specific pages. For international SEO, each target language needs its own crawlable URL plus server-visible HTML, correct `lang`, and locale-aware canonical/hreflang/sitemap handling.
